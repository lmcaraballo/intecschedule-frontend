search-users:
  get:
    summary: Search Users
#    security:
#      - ApiKey: []
    parameters:
      - name: query
        in: query
        required: false
        schema:
          type: string
    responses:
      '200':
        description: successful operation
    x-amazon-api-gateway-request-validator: Validate query string parameters and headers
    x-amazon-apigateway-integration:
      type: 'aws_proxy'
      uri: 'arn:${AWS::Partition}:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:${AWS::Partition}:lambda:${AWS::Region}:${AWS::AccountId}:function:bnbdo-user_service-${stageVariables.apiStage}/invocations'
      httpMethod: POST
      passthroughBehavior: 'when_no_match'
  options:
    x-amazon-apigateway-integration:
      requestTemplates:
        application/json: "{ \"statusCode\" : 200 }"
      type: mock
      passthroughBehavior: "when_no_match"
      responses:
        default:
          responseParameters:
            method.response.header.Access-Control-Allow-Origin: "'*'"
            method.response.header.Access-Control-Allow-Methods: "'*'"
            method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token,Token-Id'"
          responseTemplates:
            application/json: "{}\n"
          statusCode: 200
    responses:
      '200':
        description: "200 response"
        content: { }
        headers:
          Access-Control-Allow-Origin: { }
          Access-Control-Allow-Methods: { }
          Access-Control-Allow-Headers: { }
    tags:
      - CORS